<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Skrooge</title>
    <link href="public/vendor/bootstrap.min.css" rel="stylesheet" type="text/css">
</head>
<body>
    <h1>You need to categorise some merchants.</h1>
    <h3>{{currentMerchant.name}}</h3>
    <form action="/category-mapping" method="post">
        <select name="new-mapping">
            {{#each currentMerchant.categories}}
            <optgroup label="{{title}}">
                {{#each subcategories}}
                <option value="{{currentMerchant.name}},{{title}},{{name}}">{{name}}</option>
                {{/each}}
            </optgroup>
            {{/each}}
        </select>
        <input type="hidden" name="remaining-vendors" value="{{outstandingMerchants}}">
        <input type="hidden" name="originalRequestBody" value="{{originalRequestBody}}">

        <div class="button">
            <button type="submit">Update category</button>
        </div>
    </form>
    </body>
</html>